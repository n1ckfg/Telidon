// + + +   E N C O D E R   + + +

class StrokeInput {

	constructor(_color,_points, _isFill) {
		this.color = _color;
		this.points = _points;
		this.isFill = _isFill;
	}

}

class NapEncoder {

	constructor(input) {
		this.cmds = this.parseCommands(input);
	}

	parseCommands(input) {
		let returns = [];
		return returns;
	}

	makeOpcode(input) {

	}

	makeNapData(input) {

	}

	makeNapDataArray(input) {
		
	}

}

